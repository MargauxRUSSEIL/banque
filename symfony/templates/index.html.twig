{% extends 'base.html.twig' %}

{% block title %}Index{% endblock %}

{% block body %}
    {% if app.user.roles ? app.user.roles|json_encode : '' == '"ROLE_ADMIN","ROLE_USER"' %}
        <h1>Administrateur</h1>
            <a href="{{ path('app_register') }}">Ajouter un client</a>
            <a href="{{ path('user_index') }}">Liste des clients</a>
    {% endif %}

    <h1>Client</h1>
    <a href="{{ path('user_show', { 'id': app.user.id }) }}">{{ app.user.username }}</a>
    <a href="{{ path('app_logout') }}">Logout</a>

    
{% endblock %}