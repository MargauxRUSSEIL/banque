{% extends 'base.html.twig' %}

{% block title %}CompteBancaire index{% endblock %}

{% block body %}
<div class="index">
    <h1>CompteBancaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Iban</th>
                <th>Solde</th>
                <th>Propriétaire</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for compte_bancaire in compte_bancaires %}
            <tr>
                <td>{{ compte_bancaire.iban }}</td>
                <td>{{ compte_bancaire.solde }}</td>
                <td>{{ compte_bancaire.user.username }}</td>
                <td>
                    <a class="btn" href="{{ path('compte_bancaire_show', {'id': compte_bancaire.id}) }}">show</a>
                    <a class="btn" href="{{ path('compte_bancaire_edit', {'id': compte_bancaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <br/>
    <a class="btn" href="{{ path('compte_bancaire_new') }}">Ajouter un compte bancaire</a>
    <a class="btn" href="{{ path('index') }}">Accueil</a>
</div>
{% endblock %}
